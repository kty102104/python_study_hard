'''
# collections 정리

list[] : append, insert(인덱스, 값) / pop(인덱스), remove(값)
-slicing
tuple() : X
-slicing
set{} : add / remove, discard
-index 없음
dict{} : 새로운 key, value 입력 / pop(key)
-key, value 추출 메서드
'''
